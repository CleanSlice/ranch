{
  "name": "app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --port 3001",
    "build": "nuxt build",
    "build:api": "openapi-ts",
    "preview": "nuxt preview",
    "lint": "eslint ."
  }
}
