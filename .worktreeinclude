# Gitignored files copied into every new worktree (Orca, Claude Code).
# Cursor uses .cursor/worktrees.json with the same list.
.env.project
api/.env
admin/.env
app/.env
# graft's code graph — saves a `graft build` per worktree; graft refreshes it on query.
graft/
