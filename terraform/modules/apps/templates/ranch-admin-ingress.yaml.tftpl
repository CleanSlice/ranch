apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ranch-admin
  namespace: platform
  annotations:
    cert-manager.io/cluster-issuer: ${issuer}
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - ${host}
      secretName: ranch-admin-tls
  rules:
    - host: ${host}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ranch-admin
                port:
                  number: 80
