apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ranch-api
  namespace: platform
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.ranch.example.com
      secretName: ranch-api-tls
  rules:
    - host: api.ranch.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ranch-api
                port:
                  number: 3000
