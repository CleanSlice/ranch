apiVersion: v1
kind: Service
metadata:
  name: lightrag
  namespace: platform
spec:
  type: ClusterIP
  selector:
    app: lightrag
  ports:
    - port: 9621
      targetPort: 9621
      protocol: TCP
      name: http
