apiVersion: v1
kind: Secret
metadata:
  name: ghcr
  namespace: platform
type: kubernetes.io/dockerconfigjson
# data.dockerconfigjson is injected by kubectl create secret docker-registry — do not apply this file directly.
# See ranch/k8s/deploy/apply.sh
